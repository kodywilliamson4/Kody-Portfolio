import scala.io.Source
import java.io.File
import java.io.PrintWriter
import scala.io.StdIn.readLine

object Task1 {
    def main (args:Array[String]){
      
      val writer = new PrintWriter(new File("myFile.txt"))
      
      println("Enter two lines of text to write to file: ")
      val first = readline()
      val second = readline()
      
      writer.write(first)
      writer.write(second)


        fileHandler.close()
      }
}
