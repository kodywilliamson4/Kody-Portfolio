import scala.io.Source
import scala.io.StdIn.readLine

object Task3 {
    def main (args:Array[String]){
      
      val list = (5 to 54).toList.par
      list.foreach(x=> println("The cube of " + x + " is " + x*x*x))
      
      }
}
